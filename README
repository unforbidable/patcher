The Patcher is a tool that allows automatic creation of plugin files according to predefined instructions (i.e. rules).

Rules are stored in XML files and consist mostly of C# source code that is compiled and executed at run-time.
