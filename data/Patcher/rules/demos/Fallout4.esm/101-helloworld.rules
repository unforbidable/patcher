<rules>
	<rule>
		<name>Hello World</name>
		<select>
			// Print message on console
			// (Make sure tool is run with option --debug=*)
			Debug.Message("Hello World!");
		</select>
	</rule>
</rules>
