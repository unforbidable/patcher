<?xml version="1.0" encoding="UTF-8"?>
<rules>
	<rule>
		<name>Hello World</name>
		<select>
			// Print message on console 
			// (Make sure tool is run with option --debug=* or similar)
			Debug.Message("Hello World!");
		</select>
	</rule>
</rules>